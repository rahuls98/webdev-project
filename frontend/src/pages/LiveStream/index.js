import LiveStreamLayout from "../../layouts/LiveStreamLayout";

const LiveStream = () => {
    return (
        <div class="LiveStream_container">
            <LiveStreamLayout></LiveStreamLayout>
        </div>
    );
};

export default LiveStream;
