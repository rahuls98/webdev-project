import "./style.css";
import MainLayout from "../../layouts/MainLayout";

const Saved = () => {
    return (
        <div className="Saved_container">
            <MainLayout page={3} />
        </div>
    );
};

export default Saved;
